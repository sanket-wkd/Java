public class Main {
    public static void main(String[] args) {

        String name = "Sanket";
        int age = 30;
        String my_name = name;

        System.out.println("Assign 1 var value to other");
        System.out.println(my_name+name);
        System.out.println(my_name+age);
    }
}
/*
Output:-
Assign 1 var value to other
SanketSanket
Sanket30
*/
System.out.println(name, my_name); is wrong because
the println() method does not have a version that accepts two separate arguments.

Java reads this as:

"Call println() with two arguments."
But println() is defined to take only one argument (or no arguments).
Examples of valid println() methods are:


The key point is that + combines everything 
into one single String before println() is called.


/*
